name: Version Bump

on:
  push:
    branches: [main]
    paths:
      - 'ton-auditor/**'
      - '!ton-auditor/VERSION'

permissions:
  contents: write

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Bump version
        run: |
          CURRENT=$(cat ton-auditor/VERSION | tr -d '[:space:]')
          NEW=$((CURRENT + 1))
          echo "$NEW" > ton-auditor/VERSION
          echo "Version bump: ${CURRENT} -> ${NEW}"

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ton-auditor/VERSION
          git diff --cached --quiet && exit 0
          git commit -m "bump(ton-auditor): $(cat ton-auditor/VERSION)"
          git push
